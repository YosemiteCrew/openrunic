{
  "name": "openrunic",
  "version": "0.0.0",
  "private": true,
  "description": "Open-source operating system for human health",
  "license": "AGPL-3.0-only",
  "packageManager": "pnpm@10.30.1",
  "engines": {
    "node": ">=22.12"
  },
  "scripts": {
    "prepare": "husky",
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "type-check": "turbo run type-check",
    "doctor": "pnpm exec react-doctor",
    "format": "prettier --write \"**/*.{ts,tsx,js,jsx,mjs,cjs,json,md,css,yml,yaml}\"",
    "format:check": "prettier --check \"**/*.{ts,tsx,js,jsx,mjs,cjs,json,md,css,yml,yaml}\"",
    "agent:conform": "pnpm --filter @openrunic/agent run conform",
    "check:secrets": "secretlint --no-gitignore \"**/*\"",
    "verify": "pnpm run lint && pnpm run type-check && pnpm run test && pnpm run build"
  },
  "devDependencies": {
    "@commitlint/cli": "^21.2.1",
    "@commitlint/config-conventional": "^21.2.0",
    "@secretlint/secretlint-rule-no-dotenv": "^13.0.4",
    "@secretlint/secretlint-rule-preset-recommend": "^13.0.4",
    "husky": "^9.1.7",
    "lint-staged": "^16.4.0",
    "prettier": "^3.9.6",
    "react-doctor": "^0.9.11",
    "secretlint": "^13.0.4",
    "turbo": "^2.10.9",
    "typescript": "^5.9.3"
  }
}
